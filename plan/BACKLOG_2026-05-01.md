# Post-e2e-green forward backlog (2026-05-01)

> Captured 2026-05-01 after `bd8a626` (NF-batch close + doc consistency
> + CU re-baseline + state-cache hardening) landed on `origin/main`.
> E2E reaches `verified: true` end-to-end on a clean validator;
> SEC-046 CU regression gate passes (21 ixs, 0 regressions).
>
> This file is the canonical session-forward reference. Findings are
> ordered by **what gates what**, not by registry severity. Each entry
> includes engineering time, calendar time (when different), the
> remediation path, and what it depends on.
>
> When closures land, update `sec/SECURITY_REGISTRY.md` and append a
> dated session delta to `docs/CURRENT_STATE.md`. Move closed entries
> from Tier 1/2 down to a "Closed during this arc" footer rather than
> deleting them; keeps the audit trail intact.

---

## Tier 1 -- Hard mainnet blockers

Cannot deploy to mainnet without these. Everything in this tier is
either a soundness bypass we're knowingly running today
(`sec007-skip-onchain`) or a missing structural guarantee (multi-party
ceremony, multisig authorities, cross-VK replay protection).

### 1. SOLID-SEC-012 -- Multi-party trusted-setup ceremony

- **What.** Replace the single-party setup at `circuits/scripts/setup.js`
  (timestamp-entropy initial randomness) with a Powers-of-Tau-style
  multi-party ceremony. Each contributor adds their entropy and
  publishes a signed transcript; the toxic waste is destroyed by every
  honest contributor; soundness holds if at least one is honest.
- **Engineering time.** ~1 week of prep (ceremony tooling, contributor
  onboarding doc, transcript verification CI gate).
- **Calendar time.** **4-6 weeks** (longest pole on the path to mainnet).
  Coordinator + >=3 contributors + signed transcript + IPFS/Arweave
  artifact pinning.
- **Path.** Use snarkjs's standard Phase-1 + Phase-2 ceremony script.
  Coordinator publishes initial Phase-2 transcript; each contributor
  pulls, contributes, signs, pushes; final transcript becomes the new
  canonical zkey + VK. Pin sha256 in `circuits/build/verification_key.sha256`.
- **Depends on.** Tier 1 circuit changes (SEC-048 Option B, SEC-006 Part 2,
  SEC-051) need to land BEFORE the ceremony so the circuit is final.

### 2a. SOLID-SEC-048 Option B -- In-circuit BJJ prime-order subgroup check

- **What.** Replace the on-chain bypass (`sec007-skip-onchain` Cargo
  feature) with a cryptographically enforced gate the proof itself
  carries. The circuit asserts that the issuer pubkey lies in the
  prime-order subgroup by performing the cofactor-clear scalar
  multiplication in-circuit and constraining the result.
- **Engineering time.** **1-2 weeks**. The constraint count is ~30K
  (~10K for the cofactor-clear `[8] * P` and ~20K for the equality
  assertion against the input pubkey).
- **Calendar time.** Batches with the SEC-012 ceremony (the new circuit
  must be in place before the ceremony locks the zkey).
- **Path.**
  1. Add a `CofactorCleared(8)` template to
     `circuits/lib/babyjubjub.circom` that computes `[8] * P`.
  2. In `batch_credential_query.circom`, after the issuer-tree
     inclusion proof, assert `cleared.x === issuerPub.x` and
     `cleared.y === issuerPub.y` (the subgroup-check identity).
  3. Re-run trusted setup (batches with #4 + #5 below).
  4. Drop `sec007-skip-onchain` from `anchor build` in CLAUDE.md +
     `docs/DEPLOYMENT_AND_TESTING.md` runbook.
  5. Remove the feature gate from
     `programs/issuer-registry/src/lib.rs::register_issuer` (the
     bypass arm + the `Sec007Bypass` event).
- **Soundness contract.** An attacker submitting a hand-crafted tx with
  a non-subgroup pubkey now produces an invalid Groth16 proof; the
  pairing rejects on-chain. Off-chain SDK predicate becomes
  defense-in-depth, no longer load-bearing.
- **Risk.** Highest of any item in this tier. Circuit changes cascade
  through the host-side public-input contract, the wire layout, the
  WASM bridge byte format, every test fixture, and the trusted-setup
  artifact pin. Land this with **no workarounds**: if the on-chain
  check fails BPF CU, do NOT re-add the bypass; debug the circuit cost
  per L7 (byte/CU budget UP-FRONT).

### 2b. SOLID-SEC-048 Option C -- Upstream `sol_babyjubjub_*` syscall (parallel track)

- **What.** Propose a Solana SIMD adding `sol_babyjubjub_*` syscalls
  (subgroup check, point-on-curve, scalar mul) so the on-chain check
  costs ~0 CU. Long-term cleanest fix.
- **Engineering time.** ~2 weeks (RFC + reference implementation in
  agave-validator + benchmark + security review request).
- **Calendar time.** **6-12 months** through Solana governance + a
  validator-network upgrade. Out of our control beyond the proposal.
- **Path.**
  1. Draft SIMD with motivation, semantics, gas pricing.
  2. Prototype the syscall in agave-validator using arkworks/curve25519-dalek-style
     constant-time scalar mul over BN254's twisted-Edwards form.
  3. Submit for Anza review; iterate.
  4. Once shipped on mainnet, swap zk-verifier from the in-circuit
     gate (Option B) to the syscall: removes ~30K constraints from the
     circuit, lowers proving time, reduces VK size.
- **Run in parallel** to Option B; Option B ships v1, Option C ships
  v1.x. Both are proper fixes (no workarounds) -- the difference is
  cost per verify.
- **Tracking.** Open `parallel-track` checkbox under SEC-048 in
  `sec/SECURITY_REGISTRY.md`; add a Phase 5 P0 entry in
  `docs/FORWARD_ROADMAP.md` separating "ship Option B" from "advance
  Option C SIMD".

### 3. SOLID-SEC-013 / SOLID-SEC-043 -- DAO/Squads multisig authorities

- **What.** Replace single-key authorities with Squads 3-of-5 (or
  equivalent DAO threshold PDA) on:
  `RegistryConfig.authority`, `IssuerTreeBinding.operator`,
  `slash_issuer`, `submit_fraud_proof`, `transfer_authority`.
  Folds in SEC-016 (transfer_authority propose/accept).
- **Engineering time.** **2-3 weeks**.
- **Calendar time.** Independent of the ceremony; can run in parallel.
- **Path.**
  1. Pick the multisig substrate (Squads is the obvious choice).
  2. Add a propose/accept pattern: `transfer_authority` queues a
     proposal; `accept_authority_transfer` is gated by the multisig
     PDA's `signer_seeds`.
  3. Update `solid-prover` + bootstrap scripts to drive the multisig
     where needed.
  4. CI gate: a unit test that exercises the multisig PDA path
     end-to-end (propose -> threshold -> accept).
- **Risk.** Medium. Multisig PDA bugs are a real class of soundness
  issues; rely on Squads' audited implementation rather than rolling
  our own.

### 4. SOLID-SEC-006 Part 2 -- Bind `vk_generation` into circuit public inputs

- **What.** Currently `verifier_config.vk_generation` is on-chain only;
  the circuit doesn't bind it, so an attacker with cached proofs from
  generation N could replay them after rotation to generation N+1
  (modulo the freeze-gate timelock). Bind it as public-input slot 32
  (`NR_PUBLIC_INPUTS = 32 -> 33`).
- **Engineering time.** **0.5 week**.
- **Calendar time.** Batches with the SEC-012 ceremony.
- **Path.**
  1. Add `vkGeneration` input to `batch_credential_query.circom`,
     expose as the 33rd public input.
  2. Re-run trusted setup (batches with #2a + #5).
  3. Update `programs/zk-verifier/src/lib.rs`:
     - `NR_PUBLIC_INPUTS = 33`
     - new `VK_GENERATION_INPUT_INDEX = 32`
     - reconstruct slot 32 from `verifier_config.vk_generation` on-chain
       (treat as account-derived, not wire-derived).
  4. Update WIRE_INPUT_SLOTS so vk_generation is NOT on the wire; this
     is intentional -- the verifier config is the source of truth.
  5. Update holder SDK `generateBatchProof` to pass `vk_generation` as
     a circuit input; SDK reads the on-chain `verifier_config` to
     learn the current generation.

### 5. SOLID-SEC-051 -- Reject all-padding `[0,0,0,0]` proofs

- **What.** The circuit currently accepts a proof whose `schemaHashes`
  are all zero (every credential slot in padding state); the on-chain
  handler at `programs/zk-verifier/src/lib.rs:500-502` skips zero-schema
  slots, so such a proof has no semantic meaning yet verifies.
- **Engineering time.** **0.5 day** (one constraint).
- **Calendar time.** Batches with the SEC-012 ceremony.
- **Path.** Add `IsZero(schemaHashes[0]).out === 0` to
  `batch_credential_query.circom`.

---

## Tier 2 -- External-audit blockers

Auditors will not sign off without these. Every item is engineering
work, no governance/coordination required.

### 6. SOLID-SEC-010 -- Cross-language vectors 3/10 -> 10/10

- **What.** Extend `crates/solid-core/examples/gen_vectors.rs` and
  `tests/vectors/check_vectors.ts` to cover the 7 missing primitives:
  Poseidon raw (no preimage shaping), BJJ keypair gen,
  BJJ sign/verify (post-cofactor-8 fix), `derive_credential_key`,
  `computeIdentityState`, `QueryBuilder._computeContextHash`, ADR-0014
  explicit issuer-leaf vector.
- **Engineering time.** **1 week**.
- **Path.** For each primitive: produce a Rust reference vector, mirror
  the TS implementation, byte-compare in CI's
  `cross_language_vectors` job. Pin every fixture in
  `tests/vectors/*.json` with sha256.

### 7. Integration tests 02..11

- **What.** The bankrun harness is up (`tests/integration/01_registry_init.test.ts`
  exists). 9 of 11 specified scenarios are unimplemented.
- **Engineering time.** **2 weeks**.
- **Path.** Land in priority order:
  - 02 issuer lifecycle (register -> stake -> vote -> finalize -> append)
  - 06 verify happy path (Groth16 verify + nullifier PDA init)
  - 07 verify replay rejected (NullifierAlreadyExists)
  - 03 slash transfers lamports (HIGH-01 regression)
  - 04 schema and bindings (init + tree_root monotonicity)
  - 05 issue credential (SPL AC CPI + CredentialIssued event)
  - 08-09 forged tree owner-check regression
  - 10 expired credential rejected
  - 11 cross-language vectors inside suite
- **Notes.** Each scenario must run host-side via solana-bankrun; no
  external validator dependency.

### 8. SOLID-SEC-080 (NF-03) -- Schema-registry pre-CPI binding anchor

- **What.** Apply the SEC-077 pattern to schema-registry's
  `update_tree_root` + `update_global_root`: recompute Poseidon-Merkle
  root from caller-supplied `(old_leaf, leaf_index, path)` and assert
  equality with `binding.current_root` BEFORE any side effect. Closes
  the same stale-path / cross-tx-race surface that SEC-077 closed for
  issuer-registry.
- **Engineering time.** **3-5 days**.
- **CU cost.** ~50-80K additional CU per ix at depth 20.
- **Path.**
  1. Lift the `verify_*_binding_anchor` helper to a shared
     `solid_light::cpi_helpers` function (rename to
     `verify_binding_root_anchor` since it's now generic).
  2. Wire into both `update_tree_root` + `update_global_root` BEFORE
     the existing `compute_poseidon_merkle_root(new_leaf, ...)` call.
  3. Add 4+ host tests: recomputed_root_matching accepts,
     stale_path_against_advanced_binding rejects, wrong_old_leaf rejects,
     wrong_leaf_index rejects.
  4. Update CU baselines via `scripts/measure_cu.py --write-baselines`.
  5. Update `docs/CU_BUDGET.md` table.

### 9. SOLID-SEC-081 (NF-05) -- check_program_ids.py to TS literals

- **What.** Extend the existing CI gate to validate hand-rolled base58
  pubkey literals in TS files match the canonical declare_id! values.
  Currently `scripts/check_program_ids.py` only checks Anchor.toml +
  declare_id! + deployments/*.json -- TS drift is silent.
- **Engineering time.** **2 days**.
- **Path.**
  1. Grep ts-sdk/scripts for base58-shaped literals (44-char
     `[1-9A-HJ-NP-Za-km-z]+`).
  2. Allowlist exactly which TS files may name program IDs (only
     `programIds.ts` per package).
  3. Fail CI on drift.
  4. Add a Python unit test asserting the script catches a deliberate
     drift (write a test fixture with mismatched literal -> assert
     non-zero exit).

### 10. SOLID-SEC-017 -- solid-prover OsRng

- **What.** `solid-prover` uses `ark_std::test_rng()`, which is a
  deterministic PRNG seeded from a fixed value. Breaks unlinkability:
  two proofs by the same prover are correlatable.
- **Engineering time.** **0.5 day**.
- **Path.** Replace with `rand::rngs::OsRng` (or
  `rand::thread_rng()`). Add a regression test that asserts two
  back-to-back proofs have different randomness commitments.

### 11. ts-sdk unit tests baseline

- **What.** All 6 ts-sdk packages (core, holder, issuer, verifier,
  light, sdk) have jest configured but **zero `.test.ts` files**.
  Auditors will flag this.
- **Engineering time.** **1 week**.
- **Path.** At minimum:
  - `verifier::verifyOnChainV2`: happy path, replay path, buffer
    overflow path (each via a mocked RPC).
  - `holder::generateBatchProof`: commitment determinism, schema-sort
    canonicality (proof is invariant under input permutation).
  - `sdk::artifact_integrity`: hash-gate priority (env > sidecar >
    config), tamper detection, missing-pin error.
  - `core::computeSchemaHash`: cross-language vector match against the
    Rust reference (one fixture per Tier 2 #6 primitive).
  - `light::extract_active_issuer_tree_root`: parses the binding PDA;
    rejects frozen / wrong-discriminator buffers.
  - `issuer::computeIssuerLeaf`: matches the on-chain Poseidon(5)
    consumed by `append_issuer_leaf`.
- **Wire into CI:** new `sdk_jest` job in `.github/workflows/ci.yml`.

---

## Tier 3 -- Phase 4 P1 (post-audit-blocker, pre-mainnet)

Don't gate the audit but do gate mainnet (or are quality-of-life work
that's better done before mainnet rather than after).

### 12. Revocation v1 SDK helpers

- **What.** Circuit + on-chain support for revocation is in place
  (ADR-0014 + 6-input nullifier). Still missing: holder SDK helper
  (detect revocation -> bump nonce -> re-derive leaf -> request
  re-insertion), issuer SDK helper (publish revocation event + bump
  root), indexer event contract.
- **Engineering time.** **1-2 weeks**.
- **Path.** See `docs/REVOCATION_DESIGN.md` for the v1 design; the
  design is complete, the implementation is phased.

### 13. SOLID-SEC-082 (NF-06) -- Split `cooldown_ends_at`

- **What.** `IssuerAccount.cooldown_ends_at` is reused across two
  semantic states ("Cooldown ends at this slot" when status == Cooldown,
  "DAO dispute window ends" when status == Revoked, post SEC-061).
  Future refactors that touch the field without re-reading the status
  guard will corrupt one flow.
- **Engineering time.** **1 week** (layout migration).
- **Path.** Split into `cooldown_ends_at` + `dispute_window_ends_at`
  (+8 bytes to `IssuerAccount::SPACE`); each handler asserts only its
  field. Layout migration MUST batch with another on-chain change so
  we don't burn an upgrade just for this.

### 14. MODULE_CONTRACTS.md feature-row verification

- **What.** Several feature rows in `docs/MODULE_CONTRACTS.md` are
  marked BROKEN (referencing pre-Phase-3 SEC findings that have since
  closed). Verify against current code or archive.
- **Engineering time.** **2 days**.

### 15. SAS dead field decision (P3-5 / SEC-038)

- **What.** `SasAttestationBuilder.attester` is in the struct but not
  in `build_instruction_data()`'s wire format
  (`crates/solid-core/src/sas.rs:45`). Either (a) safe to remove or (b)
  a real soundness bug where attester should be part of the wire.
- **Engineering time.** Investigation 1 day, fix 0.5 day.
- **Path.** Read the SAS spec, check whether attester is supposed to
  be in the attestation payload. If yes -> add to wire format + bump
  test fixtures. If no -> remove the field + the constructor parameter.

### 16. Stale root-level audit files

- **What.** `security_audit.md` (39K) + `solid_protocol_system_audit_2026_04_21.md`
  (33K) live at the repo root; they are predecessors of the audits
  now under `sec/audits/`.
- **Engineering time.** **0.5 day**.
- **Path.** Move under `sec/audits/archive/` or delete. Either way,
  add a top-of-file note pointing to the canonical
  `sec/audits/2026-04-25_v0.6.1_deep_comprehensive_audit.md`.

---

## Tier 4 -- Governance / throughput / polish (post-mainnet)

None soundness-critical. Listed for completeness; revisit after
mainnet ships.

| ID | What | Why deferred |
|---|---|---|
| SEC-014 | Per-issuer `stake_vault` (instead of shared) | Throughput, not soundness |
| SEC-015 | `approve_via_trust_anchor` min-tier gate | Governance policy |
| SEC-016 | `transfer_authority` propose/accept | Folds with SEC-013 multisig |
| SEC-018 | `verifier_config` write-lock throughput cap | Optimization |
| SEC-019 | `set_binding_status` schema-hash reassert | Polish |
| SEC-021 | Depth-20 -> ~250K holder ceiling | Design choice; needs depth bump for >250K |
| SEC-022 | Local IsZero reimpl in `credential_atom.circom` | LOW |
| SEC-023 | `active_issuers` counter drift on Cooldown -> Revoked | LOW informational |
| SEC-024 | `unstake_tokens` raw `-=` instead of `checked_sub` | LOW |
| SEC-025 | `CheckIssuerStatus` ungated and never called on-chain | INFO dead code |
| SEC-026 | `Credential::verify_integrity` never called on-chain | INFO dead code |
| SEC-034 | `SubmitFraudProof` PDA seed constraint | Hardening |
| SEC-035 | `set_binding_status` unfreeze without timelock | Admin path |
| SEC-037 | `WithdrawAfterCooldown` authority constraint | Polish |
| SEC-038 | Master-audit informational cluster | Documentation |

---

## Recommended order this arc

1. **Tier-2 small wins** (Day 1-2, parallelizable):
   - SEC-017 (0.5 day): OsRng swap.
   - SEC-081 (2 days): TS literal drift gate.
2. **Tier-2 medium** (Day 3-5):
   - SEC-080 (3-5 days): schema-registry pre-CPI binding anchor.
3. **Tier-2 sustained** (Day 6-12):
   - ts-sdk jest baseline (1 week).
4. **Tier-1 trusted-setup batch** (Day 13-20):
   - SEC-048 Option B + SEC-006 Part 2 + SEC-051 as a single circuit
     revision; trusted setup re-run; full e2e validation; drop
     `sec007-skip-onchain` from build sequence.
5. **Tier-2 integration tests** (Day 21-35):
   - Integration tests 02..11.
6. **Tier-2 cross-language vectors** (Day 36-42):
   - SEC-010 expand 3/10 -> 10/10 primitives.

After step 4 lands, the on-chain build is sound (no bypass), and steps
5-6 can run in parallel with the multi-party ceremony coordination
(SEC-012) and the multisig integration (SEC-013/043).

---

## Discipline reminders for this arc

These are not new -- they are quoted from `plan/RESUME.md` Learnings
log because they apply directly:

- **L1.** If a fix bounces ("change X, then Y breaks, then Z breaks"),
  stop editing. Find the one definition whose wrongness produces the
  cascade and fix that.
- **L2.** Programs and circuits are the source of truth. Hierarchy:
  programs/circuits > crates > WASM bridge + TS SDK > scripts.
- **L3.** If the contract is wrong, fix the contract. No workarounds,
  no regressions. Encode invariants in the type system.
- **L4.** Add unit + integration tests + targeted msg! logs as part of
  the fix, not after. A fix without a regression gate has not shipped.
- **L7.** Before touching anything that interacts with a hard wire-size
  or CU cap, write down the byte / CU ledger explicitly UP-FRONT.
- **L8.** Separate "what I observed" from "why I think it happened".
  Don't lead with the speculative root-cause story when the fix is
  empirical.
- **L9.** When fixing a primary defect surfaces N latent bugs, each
  latent-bug fix is its own atomic commit + own regression gate + own
  registry entry. Do not bundle.

When stuck, re-read the matching learning and recent session log
(`plan/SESSION_LOG_2026-04-29.md`, `plan/SESSION_LOG_2026-04-30.md`)
before pivoting -- the same shape of problem has likely been solved.
