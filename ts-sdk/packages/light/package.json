{
  "name": "@solid-protocol/light",
  "version": "0.2.0",
  "description": "SPL Account-Compression backend adapter for the SolID protocol. Provides credential-tree creation, leaf append, root reads, and Merkle-proof retrieval. The package name is kept for API stability; the implementation is 100% SPL Account Compression.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@solana/web3.js": "^1.95.0",
    "@solana/spl-account-compression": "^0.2.1",
    "bn.js": "^5.2.1"
  },
  "devDependencies": {
    "@types/bn.js": "^5.1.5",
    "typescript": "^5.3.0"
  }
}
